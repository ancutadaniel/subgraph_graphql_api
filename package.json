{
  "name": "subgraph_test",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ subgraph_test",
    "create-local": "graph create --node http://localhost:8020/ subgraph_test",
    "remove-local": "graph remove --node http://localhost:8020/ subgraph_test",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 subgraph_test",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.39.0",
    "@graphprotocol/graph-ts": "0.29.1"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
